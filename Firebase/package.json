{
  "private": true,
  "scripts": { "test": "firebase emulators:exec --project demo-tasq-groups --config firebase.emulator.json --only firestore 'node --test groups.rules.test.cjs'" },
  "devDependencies": { "@firebase/rules-unit-testing": "5.0.2", "firebase": "12.19.0", "firebase-tools": "15.30.0" }
}
